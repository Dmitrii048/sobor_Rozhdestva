backend:
  name: git-gateway
  branch: main

media_folder: "images/news"
public_folder: "/images/news"

collections:
  - name: "news"
    label: "📰 Новости собора"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    editor:
      preview: false
    fields:
      - {label: "🔖 Категория", name: "category", widget: "select", options: ["Главная", "Воскресная школа", "Социальное служение", "Паломничество"], required: true}
      - {label: "📝 Заголовок", name: "title", widget: "string", required: true}
      - {label: "📅 Дата публикации", name: "date", widget: "datetime", required: true}
      - {label: "🖼️ Изображение (рекомендуется 1200x600px)", name: "image", widget: "image", required: true}
      - {label: "📌 Краткое описание", name: "excerpt", widget: "text", hint: "Текст, который будет отображаться в списке новостей"}
      - {label: "📄 Полный текст", name: "body", widget: "markdown", required: true}
