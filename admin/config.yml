backend:
  name: git-gateway
  branch: main # Или master, проверьте вашу ветку

media_folder: "images/news"
public_folder: "/sobor_Rozhdestva/images/news"

collections:
  - name: "news"
    label: "📰 Новости"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Категория", name: "category", widget: "select", options: ["school", "general", "social"], default: "school"}
      - {label: "Заголовок", name: "title", widget: "string"}
      - {label: "Дата", name: "date", widget: "datetime"}
      - {label: "Главное фото (Обложка)", name: "image", widget: "image"}
      - {label: "Краткое описание (для превью)", name: "excerpt", widget: "text"}
      - {label: "Полный текст новости", name: "body", widget: "markdown"}
      - label: "Галерея фото"
        name: "gallery"
        widget: "list"
        summary: "{{fields.image}}"
        field: {label: "Фото", name: "image", widget: "image"}
